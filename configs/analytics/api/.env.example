# LDS Analytics API — Environment Variables
# Copy to .env and adjust values for your setup.

# PostgreSQL connection string
DATABASE_URL=postgresql://app:app@localhost:5432/lds_analytics

# API server port
PORT=3001

# Comma-separated list of allowed origins for CORS (use * for dev)
CORS_ORIGIN=*
