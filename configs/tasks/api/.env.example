# LDS Tasks API — Environment Variables
# Copy to .env and adjust values for your setup.

# PostgreSQL connection string
DATABASE_URL=postgresql://app:app@localhost:5432/lds_tasks

# API server port
PORT=3002

# Comma-separated list of allowed origins for CORS (use * for dev)
CORS_ORIGIN=*
