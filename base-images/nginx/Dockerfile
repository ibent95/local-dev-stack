# =============================================================================
# Base image: lds/nginx — pinned Nginx runtime for static/UI workloads.
# Built from Docker Hardened Images (DHI) and reused by stack UI containers.
# =============================================================================
ARG DHI_REGISTRY=dhi.io
ARG NGINX_VERSION=1.27
FROM ${DHI_REGISTRY}/nginx:${NGINX_VERSION}
