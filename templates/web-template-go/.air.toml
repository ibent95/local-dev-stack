root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ."
  bin = "./tmp/main"
  delay = 1000
  include_ext = ["go"]
  exclude_dir = ["tmp"]
  stop_on_error = true

[misc]
  clean_on_exit = true
