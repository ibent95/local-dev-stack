# =============================================================================
# CakePHP on lds/php — overrides the global nginx vhost (docroot webroot/).
# =============================================================================
FROM lds/php:8.4
COPY configs/nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
